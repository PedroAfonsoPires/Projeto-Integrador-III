#include <stdio.h>

int main() {
    int x = 10;
    int y = 20;

    // Declaração com atribuição
    float z = 0;
    float w[];
    // If statement
    if (x < y) {
        z = x + y;
    }

    // If-else statement
    if (z > 30) {
        i++;
    } else {
        i++;
    }

    // While loop
    int counter = 0;
    while (counter < 5) {
        i++;
        counter++;
    }

    // Do-while loop
    int count_down = 5;
    do {
        i++;
        count_down--;
    } while (count_down > 0);

    // For loop
    for (int i = 0; i < 5; i++) {
        i++;
    }
    return 0;
}
